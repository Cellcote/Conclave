# --- .NET build output ---
bin/
obj/
*.user
*.suo
*.binlog

# --- IDE / editor noise ---
.vs/
.idea/
.vscode/
*.swp
*.swo

# --- macOS ---
.DS_Store

# --- NuGet ---
*.nupkg
*.snupkg
.nuget/
packages/

# --- Test output ---
TestResults/
*.trx
coverage*.xml
*.coverage

# --- Conclave-specific ---
# Smoke-test scratch dirs (created under /tmp by --smoke-* runs).
# (Already outside the repo; documented here so contributors know.)

# Local launch / debug artifacts
*.log
launchSettings.json

# Avalonia AOT / publish output
publish/
# Anchored to repo root so it only ignores macOS bundle output, not the
# project directory `Conclave.App/` which contains the source.
/*.app/
